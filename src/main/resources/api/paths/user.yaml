getUser:
  get:
    operationId: getUser
    description: Get user by id
    parameters:
      - $ref: "../components/parameters.yaml#/UserIdPath"
    responses:
      200:
        description: User model
        content:
          application/json:
            schema:
              $ref: "../components/schemas.yaml#/UserDto"
      404:
        description: User with provided id does not found
    tags:
      - user
